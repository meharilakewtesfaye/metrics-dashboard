@props(['title', 'value'])

<div class="glass-card p-4">
    <small class="block text-gray-400 mb-1.5">{{ $title }}</small>
    <div class="value font-black text-lg text-white">{{ $value }}</div>
</div>